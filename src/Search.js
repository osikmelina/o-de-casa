class Search {
  city;
  neighborhood;

  constructor(city, neighborhood) {
    this.city = city,
    this.neighborhood = neighborhood
  }

  filterByType(type) {

  }

  filterByCategory(category) {
    
  }
}